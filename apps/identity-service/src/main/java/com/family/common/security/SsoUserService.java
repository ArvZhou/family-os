package com.family.common.security;

import org.springframework.stereotype.Service;

/**
 * Synchronizes SSO user identity with the local database.
 * Reserved — will be fully implemented when SSO is enabled.
 */
@Service
public class SsoUserService {
    // SSO user sync: create or update local user from OAuth2/OIDC profile
}
